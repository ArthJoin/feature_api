services:
  postgres:
    image: postgres:16
    container_name: feature-pg
    environment:
      POSTGRES_DB: features
      POSTGRES_USER: feature_user
      POSTGRES_PASSWORD: feature_password
    ports:
      - "5433:5432"
    volumes:
      - ./postgres/data:/var/lib/postgresql/data
      - ./postgres/init:/docker-entrypoint-initdb.d
      - ./postgres/dump:/var/lib/postgresql/dump
    networks:
      - feature-net

  # duckdb:
  #   image: ghcr.io/duckdb/duckdb:latest
  #   container_name: ml-duckdb
  #   working_dir: /data
  #   volumes:
  #     - ./duckdb/data:/data
  #   # DuckDB здесь как "хранилище" для оффлайн-аналитики/ML.
  #   # Контейнер просто живёт, чтобы можно было в него заходить.
  #   command: ["tail", "-f", "/dev/null"]
  #   networks:
  #     - feature-net

  api:
    build:
      context: ./services
      dockerfile: Dockerfile
    container_name: feature-api
    depends_on:
      - postgres
    environment:
      POSTGRES_DSN: postgres://feature_user:feature_password@feature-pg:5432/feature_store?sslmode=disable
      PORT: "9000"
    ports:
      - "9000:9000"
    networks:
      - feature-net

  camunda:
    image: camunda/camunda-bpm-platform:latest
    container_name: camunda
    ports:
      - "8080:8080"
    environment:
      # опционально, но удобно
      - CAMUNDA_BPM_HISTORY_LEVEL=full
    # если нужен общий network с твоим сервисом:
    volumes:
      - ./camunda-data:/camunda/configuration/resources
    networks:
      - feature-net

networks:
  feature-net:
    driver: bridge
